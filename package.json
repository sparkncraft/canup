{
  "name": "canup-sdk",
  "version": "0.0.0",
  "description": "CanUp SDK monorepo — @canup/ui React components and @canup/cli developer tool.",
  "license": "MIT",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sparkncraft/canup.git"
  },
  "packageManager": "pnpm@10.30.2",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:build": "publint --strict packages/ui && publint --strict packages/cli && attw --pack packages/ui --profile esm-only && attw --pack packages/cli --profile esm-only",
    "size": "size-limit",
    "size:check": "size-limit --limit",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "changeset": "changeset",
    "prepare": "simple-git-hooks"
  },
  "simple-git-hooks": {
    "pre-commit": "branch=$(git rev-parse --abbrev-ref HEAD); if [ \"$branch\" = \"main\" ]; then echo '\\n❌ Cannot commit directly to main. Create a feature branch first.\\n' >&2; exit 1; fi && pnpm lint-staged",
    "pre-push": "while read local_ref rest; do case \"$local_ref\" in refs/tags/*) ;; *) branch=$(git rev-parse --abbrev-ref HEAD); if [ \"$branch\" = \"main\" ]; then echo '\\n❌ Cannot push directly to main. Use a branch + PR.\\n' >&2; exit 1; fi ;; esac; done"
  },
  "devDependencies": {
    "@arethetypeswrong/cli": "^0.18.2",
    "@canup/types": "^0.2.0",
    "@canva/app-i18n-kit": "^1.2.0",
    "@canva/app-ui-kit": "^5.9.0",
    "@canva/platform": "^2.2.2",
    "@canva/user": "^2.2.0",
    "@changesets/changelog-github": "^0.6.0",
    "@changesets/cli": "^2.30.0",
    "@eslint/js": "^10.0.1",
    "@prettier/plugin-oxc": "^0.1.3",
    "@size-limit/preset-small-lib": "^12.0.1",
    "@testing-library/react": "^16.3.2",
    "@types/node": "^22.13.4",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitest/coverage-v8": "^4.1.2",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "jsdom": "^26.0.0",
    "lint-staged": "^16.4.0",
    "msw": "^2.12.14",
    "prettier": "^3.8.1",
    "publint": "^0.3.18",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-intl": "^7.1.11",
    "simple-git-hooks": "^2.13.1",
    "size-limit": "^12.0.1",
    "tsdown": "^0.21.4",
    "type-fest": "^5.5.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.58.0",
    "vitest": "^4.1.0"
  }
}
